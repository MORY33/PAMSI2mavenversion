import org.testng.Assert;
import org.testng.annotations.Test;

import java.io.IOException;

public class ListTest {
    @Test
    public void testList() throws IOException {
//        Movie tempList = new Movie();
//        tempList.CreateList(10);
//
//        //Testing if first field of list equals to predefined
//        Assert.assertEquals(tempList.CreateList(10).get(0).getRating(), 9);
//
//        //Testing if last field of list equals to predefined
//        Assert.assertEquals(tempList.CreateList(10).get(9).getRating(), 2);
    }
}
